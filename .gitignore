node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
coverage
*.tsbuildinfo
.env

# In case BACKUP_DIR is ever pointed inside the repo (default is outside it —
# see scripts/README-backups.md). These contain sensitive data.
DMSD-Schedule-Backups/

# Local Supabase CLI cache (linked project ref, versions) — machine-specific.
supabase/.temp/
